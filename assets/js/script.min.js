$(document).ready(function () {
        AOS.init({
                disable: "mobile"
            }),
            function () {
                if ("requestAnimationFrame" in window && !/Mobile|Android/.test(navigator.userAgent)) {
                    var n = [];
                    if ($("[data-bs-parallax-bg]").each(function () {
                            var o = $(this),
                                e = $("<div>");
                            e.css({
                                backgroundImage: o.css("background-image"),
                                backgroundSize: "cover",
                                backgroundPosition: "center",
                                position: "absolute",
                                height: "200%",
                                width: "100%",
                                top: 0,
                                left: 0,
                                zIndex: -100
                            }), e.appendTo(o), n.push(e[0]), o.css({
                                position: "relative",
                                background: "transparent",
                                overflow: "hidden"
                            })
                        }), n.length) {
                        var o, e = [];
                        $(window).on("scroll resize", t), t()
                    }
                }

                function t() {
                    e.length = 0;
                    for (var t = 0; t < n.length; t++) {
                        var i = n[t].parentNode.getBoundingClientRect();
                        i.bottom > 0 && i.top < window.innerHeight && e.push({
                            rect: i,
                            node: n[t]
                        })
                    }
                    cancelAnimationFrame(o), e.length && (o = requestAnimationFrame(a))
                }

                function a() {
                    for (var n = 0; n < e.length; n++) {
                        var o = e[n].rect,
                            t = e[n].node,
                            a = Math.max(o.bottom, 0) / (window.innerHeight + o.height);
                        t.style.transform = "translate3d(0, " + -50 * a + "%, 0)"
                    }
                }
            }()
    }),
    function (n) {
        "use strict";
        n('a.js-scroll-trigger[href*="#"]:not([href="#"])').click(function () {
            if (location.pathname.replace(/^\//, "") == this.pathname.replace(/^\//, "") && location.hostname == this.hostname) {
                var o = n(this.hash);
                if ((o = o.length ? o : n("[name=" + this.hash.slice(1) + "]")).length) return n("html, body").animate({
                    scrollTop: o.offset().top - 54
                }, 1e3, "easeInOutExpo"), !1
            }
        }), n(".js-scroll-trigger").click(function () {
            n(".navbar-collapse").collapse("hide")
        }), n("body").scrollspy({
            target: "#mainNav",
            offset: 56
        });
        var o = function () {
            n("#mainNav").offset().top > 100 ? n("#mainNav").addClass("navbar-shrink") : n("#mainNav").removeClass("navbar-shrink")
        };
        o(), n(window).scroll(o), n(".portfolio-modal").on("show.bs.modal", function (o) {
            n(".navbar").addClass("d-none")
        }), n(".portfolio-modal").on("hidden.bs.modal", function (o) {
            n(".navbar").removeClass("d-none")
        })
    }(jQuery), $(function () {
        new Swiper(".swiper-container", {
            loop: !0,
            pagination: ".swiper-pagination",
            paginationClickable: !0,
            nextButton: ".swiper-button-next",
            prevButton: ".swiper-button-prev"
        })
    });